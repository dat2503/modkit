{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "SEARCH_PROVIDER": {
      "type": "string",
      "enum": ["elasticsearch"],
      "description": "Search provider to use"
    },
    "ELASTICSEARCH_URL": {
      "type": "string",
      "description": "Elasticsearch endpoint URL (e.g. http://localhost:9200 or https://your-cluster.es.io)"
    },
    "ELASTICSEARCH_API_KEY": {
      "type": "string",
      "description": "Elasticsearch API key for authentication (preferred over username/password)",
      "sensitive": true
    },
    "ELASTICSEARCH_INDEX_PREFIX": {
      "type": "string",
      "description": "Prefix for all index names (e.g. 'myapp_'). Useful for sharing a cluster across environments.",
      "default": ""
    }
  },
  "required": ["SEARCH_PROVIDER", "ELASTICSEARCH_URL"]
}
