# .codecov.yml
# Generated by modkit cicd module — compliance posture: enterprise
coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 1%    # tight — only 1% drop allowed
    patch:
      default:
        target: 80%

comment:
  layout: "reach,diff,flags"
  behavior: default
  require_changes: true
