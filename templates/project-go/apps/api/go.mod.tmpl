module {{.GoModule}}

go {{.GoVersion}}

require (
	github.com/dat2503/modkit v0.0.0  // replace with pinned version after scaffold
)

// Module SDK dependencies are added by `go get` after scaffold.
// Run: make deps
