/** @type {import('next').NextConfig} */
const nextConfig = {
  // API base URL — set at build time or via NEXT_PUBLIC_API_URL env var
  env: {
    NEXT_PUBLIC_API_URL: process.env.NEXT_PUBLIC_API_URL || 'http://localhost:8080',
  },
}

module.exports = nextConfig
